"use client";

import React from "react";
import QuizHome from "../../src/pages_components/QuizHome";

// Public CBT entry — the student quiz-taking path, copied verbatim from the
// admin project's /quiz route (same components, same data tables).
export default function CbtPage() {
  return <QuizHome />;
}
